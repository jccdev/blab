import { AuthForm } from 'shared/models/auth-form.ts';
import { AuthUser } from 'shared/models/auth-user.ts';

export class AuthService {
  authenticate(authForm: AuthForm) : AuthUser {
    return null;
  }
}